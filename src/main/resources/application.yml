server:
  port: 8088

logging:
  level:
    org.springframework: DEBUG

Spring:
  messages:
    basename: messages
  security:
    user:
      name: username
      password: passw0rd
  mvc:
    path match:
      matching-strategy: ant_path_matcher

management:
  endpoints:
    web:
      exposure:
        include: "*"
